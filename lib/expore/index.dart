import 'package:flutter/material.dart';

class ExploreIndex extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Center(
      child: Text('explore_index'),
    );
  }
}